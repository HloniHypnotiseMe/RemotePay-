# 🔥 RemotePay - Complete Payment Gateway

Production-ready payment gateway with PayFast, AI assistant, and merchant dashboard.

## Quick Start

```bash
# Backend
cd backend && python -m venv venv
source venv/bin/activate && pip install -r requirements.txt
python main.py

# Frontend (new terminal)
cd frontend && npm install && npm run dev

